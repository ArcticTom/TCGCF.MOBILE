//app entry point

import { AppRegistry } from 'react-native';
//import App.js root component
import App from './App';

//render to the screen
AppRegistry.registerComponent('TestProject', () => App);
