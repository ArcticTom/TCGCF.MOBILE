import { BORDER_COLOR } from '../styles/common';

const styles = {
  listStyle: {
    borderColor: BORDER_COLOR,
    borderBottomWidth: 1,
    padding: 5,
    paddingLeft: 20,
    paddingRight: 20,
    flexDirection: 'row'
  }
};

export default styles;
